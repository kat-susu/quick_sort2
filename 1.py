'''
Задание 1.

Массив                        Pivot первый           Pivot последний           Pivot средний

Отсортированный            1 (худший случай)        7 (худший случай)        4 (лучший случай)
Обратно отсортированный    7 (худший случай)        1 (худший случай)        4 (лучший случай)
Случайный порядок          4 (средний случай)       5 (средний случай)       3 (средний случай)


Вопросы:

1. Этот выбор неэффективен на уже отсортированных или обратно отсортированных массивах, 
так как в качестве элемента pivot всегда выбирается крайнее значение (минимум или максимум),
из-за чего массив делится максимально несбалансированно, а скорость работы алгоритма падает до худшего значения O(n^2).

2. Выбор последнего элемента приводит к худшему случаю O(n^2) на отсортированных и обратно отсортированных массивах,
поскольку pivot снова оказывается максимумом или минимумом и делит массив на пустую часть и оставшиеся n-1 элементов.

3. Нет, не всегда, так как это правило смотрит лишь на позицию элемента, а не на его значение;
если на среднем индексе случайно окажется самое маленькое или самое большое число всего массива (например, [1, 2, 9, 3, 4], где в центре стоит 9),
разбиение получится крайне неэффективным.

4. Средний индекс — это просто центральная позиция в текущем массиве, которая вычисляется математически (left + right) / 2 без учета самого значения,
в то время как медиана значений — это число, которое делит элементы строго пополам (половина меньше него, половина больше),
что гарантирует идеальный баланс при разбиении, но требует предварительной сортировки или поиска.
'''
